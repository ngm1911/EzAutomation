<Window x:Class="AutomationTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutomationTool"
        xmlns:helper="clr-namespace:AutomationTool.Helper"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:vm="clr-namespace:AutomationTool.ViewModel"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="MainWindow" Height="550" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Column="0" LastChildFill="True">
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center">
                <DockPanel>
                    <Button DockPanel.Dock="Right" 
                        Command="{Binding RunCommand}"
                        CommandParameter="{Binding}"
                        Margin="5"
                        Height="50">
                        <Image Source="/Icon/Start_32.png" Width="32" Height="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    
                    <DockPanel.Style>
                        <Style TargetType="DockPanel">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ProcessingQueue}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                </DockPanel>
                
            </StackPanel>

            <TreeView ItemsSource="{Binding AutoTree}" MouseDoubleClick="TreeView_MouseDoubleClick">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                        <Setter Property="IsExpanded" Value="True"/>
                    </Style>
                </TreeView.ItemContainerStyle>

                <TreeView.ContextMenu>
                    <ContextMenu Width="120">
                        <MenuItem Header="Collapse All" Click="btnCollapseAll_Click" Margin="-20,0,0,0"/>
                        <MenuItem Header="Expand All" Click="btnExpandAll_Click" Margin="-20,0,0,0"/>
                    </ContextMenu>
                </TreeView.ContextMenu>
                
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <StackPanel Margin="3">
                            <DockPanel>
                                <Border Background="White" CornerRadius="5" DockPanel.Dock="Right">
                                    <StackPanel Orientation="Horizontal" Height="20" >
                                        <ToggleButton x:Name="btnEdit"
                                                      Background="Transparent"
                                                      BorderBrush="Transparent"
                                                      DockPanel.Dock="Right" 
                                                      Command="{Binding DataContext.SaveCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                      Visibility="Collapsed"
                                                      Padding="3">
                                            <Image Width="16" Height="16">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Setter Property="Source" Value="/Icon/Edit.png"/>
                                                        <Setter Property="ToolTip" Value="Edit process"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}}" Value="True">
                                                                <Setter Property="Source" Value="/Icon/Save.png"/>
                                                                <Setter Property="Margin" Value="2"/>
                                                                <Setter Property="ToolTip" Value="Save process"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>
                                        </ToggleButton>
                                        <Image Width="18" Height="18" Stretch="Uniform" Margin="2,0,3,0" >
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Passed">
                                                            <Setter Property="Source" Value="/Icon/Check.png"/>
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Error">
                                                            <Setter Property="Source" Value="/Icon/Alert.png"/>
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Setter Property="ToolTip" Value="{Binding Error}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Running">
                                                            <Setter Property="Source" Value="/Icon/Loading.png"/>
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>
                                        </Image>

                                        <Button ToolTip="Move up"
                                                BorderBrush="Transparent"
                                                Command="{Binding DataContext.UpStepCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Padding="0"
                                                Width="20" 
                                                Height="20"
                                                VerticalAlignment="Center">
                                            <Image Source="/Icon/Up.png"/>
                                        </Button>

                                        <Button BorderBrush="Transparent" 
                                                ToolTip="Move down"
                                                Command="{Binding DataContext.DownStepCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                CommandParameter="{Binding}"
                                                Padding="0"
                                                Width="20" 
                                                Height="20"
                                                VerticalAlignment="Center" >
                                            <Image Source="/Icon/Up.png">
                                                <Image.RenderTransform>
                                                    <RotateTransform Angle="180" CenterX="9" CenterY="9"/>
                                                </Image.RenderTransform>
                                            </Image>
                                        </Button>
                                    </StackPanel>
                                </Border>
                                <CheckBox IsChecked="{Binding Selected}" Margin="0,0,5,0"/>
                                <TextBlock x:Name="txbGroupName" Visibility="Visible" Text="{Binding Name}" VerticalAlignment="Center"/>
                                <TextBox x:Name="txtGroupName" Margin="0,0,2,0" Visibility="Collapsed" Text="{Binding Name}" VerticalAlignment="Center"/>
                            </DockPanel>

                            <hc:ButtonGroup x:Name="editGroup" Visibility="Collapsed" 
                                            Width="213"
                                            Margin="0,2,0,0"
                                            HorizontalAlignment="Right">
                                <Button Command="{Binding AddItemCommand}"
                                        BorderBrush="Transparent"
                                        DockPanel.Dock="Right" 
                                        Padding="-1"
                                        ToolTip="New process">
                                    <Image Source="/Icon/Add.png" Width="16" Height="16"/>
                                </Button>

                                <Button Command="{Binding DeleteItemCommand}"   
                                        BorderBrush="Transparent"
                                        ToolTip="Remove process">
                                    <Image Source="/Icon/Delete.png" Width="16" Height="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Button>

                                <Button Command="{Binding CopyItemCommand}"
                                        BorderBrush="Transparent"
                                        DockPanel.Dock="Right" 
                                        ToolTip="Copy process">
                                    <Image Source="/Icon/Copy.png" Width="16" Height="16"/>
                                </Button>

                                <Button Command="{Binding PasteItemCommand}"
                                        BorderBrush="Transparent"
                                        DockPanel.Dock="Right" 
                                        ToolTip="Paste process">
                                    <Image Source="/Icon/Paste.png" Width="16" Height="16"/>
                                </Button>
                            </hc:ButtonGroup>
                        </StackPanel>

                        <HierarchicalDataTemplate.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=TreeViewItem}, Path=IsMouseOver}" Value="True">
                                <Setter TargetName="btnEdit" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsChecked, ElementName=btnEdit}" Value="True">
                                <Setter TargetName="txtGroupName" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="editGroup" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="txbGroupName" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="btnEdit" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsChecked, ElementName=btnEdit}" Value="False">
                                <Setter TargetName="txbGroupName" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="txtGroupName" Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </HierarchicalDataTemplate.Triggers>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </DockPanel>
        
        <GridSplitter Grid.Column="1" Width="3"                   
              HorizontalAlignment="Center"
              VerticalAlignment="Stretch"
              Background="Gray"
              ShowsPreview="True"  />

        <TabControl Grid.Column="2" x:Name="tabControl">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <StackPanel.ContextMenu>
                            <ContextMenu Width="120">
                                <MenuItem Header="Close all" Click="btnCloseAllTab_Click" Margin="-20,0,0,0"/>
                            </ContextMenu>
                        </StackPanel.ContextMenu>
                        <TextBlock Text="{Binding Name}" Margin="0,0,5,0"/>
                        <Button x:Name="btnCloseTab"
                                Click="btnCloseTab_Click"
                                BorderThickness="0"
                                Padding="0"
                                Width="12" 
                                Height="12"
                                VerticalAlignment="Center">
                            <Image Source="/Icon/Close.png" Width="10" Height="10"/>
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>

            <TabControl.ContentTemplate>
                <DataTemplate>
                    <DockPanel LastChildFill="True">
                        <DataGrid ItemsSource="{Binding Steps}" 
                                  SelectionMode="Single" SelectionUnit="FullRow" 
                                  AutoGenerateColumns="False" 
                                  CanUserSortColumns="False">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn>
                                    <DataGridTemplateColumn.HeaderTemplate>
                                        <DataTemplate>
                                            <hc:ButtonGroup >
                                                <Button DockPanel.Dock="Right" 
                                                        Command="{Binding DataContext.SaveCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                        ToolTip="Save process">
                                                    <Image Source="/Icon/Save.png" Width="16" Height="16"/>
                                                </Button>
                                                <Button DockPanel.Dock="Right" 
                                                        Command="{Binding DataContext.AddStepCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                        ToolTip="Add step">
                                                    <Image Source="/Icon/Add.png" Width="16" Height="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Button>
                                            </hc:ButtonGroup>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.HeaderTemplate>
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <hc:ButtonGroup >
                                                <Button BorderBrush="Transparent"  Padding="4"
                                                        Command="{Binding DataContext.ExcuteStepCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                        CommandParameter="{Binding}" ToolTip="Excute step">
                                                    <Image Source="/Icon/Start.png" Width="16" Height="16"/>
                                                </Button>
                                                <Button BorderBrush="Transparent" 
                                                        Padding="2" ToolTip="Move up"
                                                        Command="{Binding DataContext.UpStepCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                        CommandParameter="{Binding}" >
                                                    <Image Source="/Icon/Up.png" Width="16" Height="16"/>
                                                </Button>
                                                <Button BorderBrush="Transparent" Padding="2" ToolTip="Move down"
                                                        Command="{Binding DataContext.DownStepCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                        CommandParameter="{Binding}" >
                                                    <Image Source="/Icon/Up.png"   Width="16" Height="16">
                                                        <Image.RenderTransform>
                                                            <RotateTransform Angle="180" CenterX="8" CenterY="8"/>
                                                        </Image.RenderTransform>
                                                    </Image>
                                                </Button>
                                                <Button BorderBrush="Transparent" Padding="4"
                                                        Command="{Binding DataContext.RemoveStepCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                        CommandParameter="{Binding}">
                                                    <Image Source="/Icon/Delete.png" Width="16" Height="16"/>
                                                </Button>

                                                <Button BorderBrush="Transparent" Padding="4"
                                                        Command="{Binding DataContext.CheckElementCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                                        CommandParameter="{Binding}">
                                                    <Image Source="/Icon/Point.png" Width="16" Height="16"/>
                                                </Button>
                                            </hc:ButtonGroup>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Header="Status">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Image Width="20" Height="20" Stretch="Uniform">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Passed">
                                                                <Setter Property="Source" Value="/Icon/Check.png"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Error">
                                                                <Setter Property="Source" Value="/Icon/Alert.png"/>
                                                                <Setter Property="ToolTip" Value="{Binding Error}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Running">
                                                                <Setter Property="Source" Value="/Icon/Loading.png"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                
                                <DataGridTemplateColumn Header="Control">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding ControlType}" >
                                                <TextBlock.ContextMenu >
                                                    <ContextMenu  Width="120" >
                                                        <MenuItem Header="Clear cache"
                                                                  Margin="-20,0,0,0"
                                                                  Command="{Binding ClearCacheCommand}"/>
                                                    </ContextMenu>
                                                </TextBlock.ContextMenu>
                                            </TextBlock>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <hc:ComboBox ItemsSource="{Binding ControlItemSource}"
                                                         SelectedItem="{Binding ControlType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Header="Action">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding ActionType}" >
                                                <TextBlock.ContextMenu >
                                                    <ContextMenu  Width="120" >
                                                        <MenuItem Header="Clear cache"
                                                                  Margin="-20,0,0,0"
                                                                  Command="{Binding ClearCacheCommand}"/>
                                                    </ContextMenu>
                                                </TextBlock.ContextMenu>
                                            </TextBlock>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <hc:ComboBox ItemsSource="{Binding ActionItemSource}"
                                                         SelectedItem="{Binding ActionType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                
                                <DataGridTextColumn Width="200" Header="Param 0" Binding="{Binding Param0, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <DataGridTextColumn Width="200" Header="Param 1" Binding="{Binding Param1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <DataGridTextColumn Width="150" Header="Param 2" Binding="{Binding Param2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <DataGridTextColumn Width="70" Header="Param 3" Binding="{Binding Param3}" Visibility="Collapsed"/>
                                <DataGridTextColumn Width="70" Header="Param 4" Binding="{Binding Param4}" Visibility="Collapsed"/>
                                <DataGridCheckBoxColumn Header="SkipError"
                                                        Binding="{Binding SkipError}"/>
                                <DataGridTextColumn Header="Notes" Binding="{Binding Name}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</Window>
